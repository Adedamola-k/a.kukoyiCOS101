use std::io;

fn main() 
{
    //display
    let r = 15000;
    let a = 12500;
    let d = 20000;
    let n = 18000;

    //ask for book codes 
    println!("enter your book code:");
    i8::stdin().read_line(&mut input1).expect("not a valid string");
    let mut input1 = string::new;
    let book code:f32 = input1.trim().parse().expect("not a valid number");

    //ask for book quantity
    println!("enter your book quantity:");
    let mut input2 = String::new();
    io::stdin().read_line(&mut input2).expect("not a valid string");
    let book quantity:i32 = input2.trim().parse().expect("not a valid number");

    //total amount
    let t = r + a + d + n;
    println!("total amount is {}", t );

    if book quantity ==<3
    let t = (10 / 100) * y
{
    println!("your total amount is {}", t );
}



}
